# 2026-09 会话归档

## 09-06 最终验收失败后的受控恢复闭环

- 根因：旧 `run_modeling_task_async` / `run_resume_task_async` 在 `_finalize_docx_and_manifest` 返回 `TECHNICAL_FAIL` 后直接写 `failed`，只报告失败，不区分可确定性导出问题与执行/数学/匿名实质问题。
- 修复：`modeling_router.py` 增加失败项 allowlist；仅当 final acceptance 和 submission audit 失败项均属于导出/报告新鲜度集合时，登记一次持久化 `presentation_reflow_pending_export`，调用既有隔离检查点恢复，跳过 Provider、Coder、Notebook 和求解器，再完整重建 Markdown、DOCX、PDF、LaTeX sidecar、manifest、submission audit、final acceptance。第二次失败或硬门禁失败立即保持 `failed`。
- 回归：`test_task_recovery`、`test_task_state_diagnosis`、`test_user_output_and_tasks` 48 项通过；目标文件 Ruff 通过。文档同步 `STARTUP.md`、`docs/md/PDF模板导出说明.md`、`docs/md/CUMCM_FINAL_REVIEW_CHECKLIST.md`。

## 09-06 算法/匿名门禁修复分支

- `fix/algorithm-evidence-ledger-v2` 在隔离 worktree 完成算法证据语境修复：当前方法声明仍硬阻断，未来/比较/待复算语境进入 `excluded_claims`，并复用最终代码附录源码集合。
- `submission_audit.py` 的 manifest 匿名扫描现在跳过 `sha256`/`sha1`/`md5`/`hash`/`digest` 等校验字段，仅检查提交文件名、显式身份字段和文件路径；PDF/DOCX 正文、元数据和真实文件名保持高置信阻断。
- 验证：`test_submission_audit` 39 项通过；算法、后处理、结果完整性、工作流、候选修复、论文修订联合回归 236 项通过；目标文件 Ruff 通过。
- LP 任务 `20260906-033157-bd8195551a4a760570d999c09194c6ab` 刷新匿名审计后 `submission_anonymity=PASS`，但并发进程随后更新 `res.md/res.json`（mtime 13:33），使 manifest/final acceptance 旧哈希失配；任务状态仍 `failed`，因此未继续重绑或导出。冻结结果 SHA 前缀 `ea38a3c6...`、执行证据 SHA 前缀 `bb30986d...` 未变。

## 09-02 第五轮：P0-P3 瑕疵修复与门禁再收敛（task=20260830-234433-4b3226317d54a94062be7a3379cf1a10）

**背景**：第四轮收敛到 TECHNICAL_PASS 后，外部审查确认 5 项属实并给出 P0-P3 清单。P0（图注
100 口径三处 + ques4_uncertainty_ci.png 双面板重绘）已由上一轮完成；本轮执行 P1/P2/P3 并再收敛。

**P1 代码同步与真实重跑**
1. 精确 diff 定位：notebook cell2(lp_core) 缺 1444 字符 `calibrate_task_power_scale`（AI 负荷按
   来源区域标定，任务级重建比附件 Baseline 系统性偏高 ~14%）；cell8(q4_model) 差 1864 字符
   （MC_N 50→100、AI 负荷标定、非支配判定从二维 (cost,carbon) 升级为三维 (cost,carbon,utilization)）。
   cell4(q2)/cell6(q3) 与 audit 文件逐字一致。
2. 替换 cell2←lp_core.py、cell8←q4_model.py（备份 internal/audit_20260901/notebook.bak_pre_p1.ipynb）。
3. jupyter_client 8.6.3 + venv python3 kernelspec 手写执行循环（rerun_cells.py），CWD=work_dir，
   每格执行前注入 __file__=audit 文件路径。真实重跑 cell2(42s)/cell6(176s)/cell8(662s，12 情景+100 MC
   缓存全命中)；cell4(Q2, 3481s) 按简报跳过（源码一致、输出与冻结吻合）。
4. **确定性验证**：重跑后 17 项冻结 CSV 源全部字节级一致（仅 notebook.ipynb 哈希合法变化）；
   MC 100 样本 mean=1,284,013,382.64、CI 半宽比独立复算=0.0457，与冻结逐字吻合。

**受控重绑**：frozen_results.json 中 notebook.ipynb 哈希 9017b48b→c3e19dac（唯一变化源），
随后 reconcile_gates2.py 全链重收敛 → 六门禁全绿。

**P2**：cell2 的 ε=0.7 单区域诊断行（cost=226,495,937,439）在 lp_core.py 源码加注释：单区域碳
约束在"消纳份额+进口上限"下不可行，226B 是进口松弛(1e6 CNY/MW)罚信号而非可行解；全局碳约束
由 Q2 迁移层处理。注释随附录入文，消除评审误读面。

**P3**：res.md L458 插入口径脚注：情景区分度 0.7408（12 情景成本极差/均值，sc_range）与 MC 成本
极差比 0.8025（100 样本 (max-min)/mean，scenario_range_ratio）口径不同、互补不替代。插入时
曾引入 0x08 控制符（heredoc 转义），已修复为 \bar{C}。

**附录与导出**：fix_appendix.py --apply（附录新增标定函数与三维非支配代码，per-cell SHA
97b6126b4a11…）→ fix_support_caption.py 补回"表7"表题（append_code_appendix 每次重建都会
吃掉该表题，已确认为必跟步骤）→ regen_pdf.py 重导 res.pdf **71 页**（正文 30，附录 33；
新增代码 +3 页）→ reconcile_gates2.py（backend CWD）。

**终态核验（独立对盘）**：final_acceptance=TECHNICAL_PASS 12/12；preflight/pdf_visual(71页,
body 30)/submission_audit/cross_modal/execution_validation 全 PASS；res.md/json/docx/pdf/
frozen_results 五件与 candidate_manifest 逐哈希 MATCH；"Monte Carlo (50)"/"50个抽样"/"1.33"
零残留；notebook outputs 含"情景12 MC100"执行痕迹。

**已知边界**：listings 环境缺 λ/≤/≥ 字形（lmmono 警告，附录代码注释装饰性缺字）；human_review
仍 PENDING_HUMAN_REVIEW（四项人工复核未做）；git 工作树未提交（待用户指令）。

## 09-03 精修轮详文（主文件仅存结论行）

外审按 71 页最新版复核给 86-89 分，指出四处"方法表述比真实算法高一级"的问题。本轮全部落地：

1. **Q2 主算法改名**：正文 12 处 ε-约束表述统一为"碳价参数化加权标量化（λ∈{0,200,500,900,1500} CNY/tCO₂）+ LP 对偶边际定价引导迁移"；5.2.2 重写方法动机（凸问题下与 ε-约束刻画同一前沿，价格形式与对偶定价自然耦合）；问题分析、总策略、结果段、结论、7.1 同步。图4 图注改"碳价扫描单调性检验"。
2. **Q2 真实非支配筛选**：q2_model.py 加 (cost,carbon) 二维支配检验（baseline、lam200、lam500 被筛除，非支配 = {lam0, lam1500}）；ques2_pareto_frontier.csv 加 nondominated 列；frozen pareto_point_count 5.0→2.0（label 改"候选集内非支配代表点数"）；ques2_acceptance_metrics.csv 同步；notebook cell4 与 audit 脚本逐字一致 + 筛选输出行 + frozen notebook 哈希重绑。正文图2 段写明"6 候选经支配检验保留 2 个非支配构成前沿"。
3. **Q3 循环寿命约束披露**：正文新增 Σchg ≤ N^cycle·(2407/24)·Capacity（N^cycle=1.5 次/日）+ 标定依据（无约束时 LP 套利吞吐 4050.9 GWh = 基准 587 GWh 的 6.9 倍且西部成本为负；基准反算 0.64-0.83 次/日，上浮取 1.5）+ 敏感性（1.0→约2.1亿、1.5→2.37亿、2.0→约2.6亿，结论不敏感）。
4. **Q4 降格**：全文从"六目标联合优化"改为"固定问题二迁移方案下的多情景电力层再优化"；时延/服务质量为描述性指标，非支配检验在成本-碳排-利用率三维执行（6/12 非支配不变）；MC 声明六区域聚合代理口径（情景仍逐区域精确重解）。
5. **统计术语拆分**：0.0457 全文改称"成本均值95%置信区间相对半宽（正态近似 1.96s/(√n·C̄)）"；QoS Wilson 95% CI [0.9999,1.0000] 单列；±2.94% 矛盾改 ±4.57%；6.1.3 脚注保留 0.7408 vs 0.8025 口径区分。关键词换为"多目标优化；线性规划；碳感知调度；蒙特卡洛模拟；灵敏度分析"（删无对应的"非线性规划/鲁棒优化"）。

连带修复：7.1 段被上轮压缩误截断的句子补全；7.2 缺点从"LP松弛+取整修复MILP"（旧方法，与实际不符）改写为真实局限（聚合贪心启发式非全局最优、MC 聚合代理、确定性序列处理），并删除与 Q4 矛盾的"确定性假设未考虑"句；6.3 待复算项压缩为三条一句式。

页数控制：语义扩充+约1500字曾使正文 31 页超限；先删 6 条冗余图引句导致 figure_references 条件项（全部 17 图须被正文引用），恢复后改用压缩法——6.2 优势段、6.3、问题重述四问段、7.3 第三点，最终正文回到 30 页整。

终态：res.pdf 72 页（正文 30 + 参考文献至附录 40 页）、六门禁 TECHNICAL_PASS 12/12、五件哈希 MATCH、新表述在 PDF 第 3/22 页等处渲染确认、首页完整。AI 痕迹审计 24.5 分的 P0 改进亦已在前轮完成。

## 09-03 LNS 升级实测详文（主文件仅存结论）

执行链：宿主venv证伪"沙箱OOM不可行"（solve_lp 18.2s、MILP 63768vars 22s/次）→ v2 贪心+修复
生成 50k 起始调度（迁移率 0.763，repair log 7178 条）→ LNS driver 三次迭代修通（BASE 别名、
TIME_LIMIT 300→900s、TIEBREAK_GAP 1e-6→5e-4、OPERATORS 裁剪单算子、膝点 a1b 池内回退）→
单算子 marginal_cost 产出 7 个 MILP 精确候选，全部 0 时延/GPU/能量违反、功率残差 5.7e-14。

**缝合验证（决定性）**：把 a1b anchor_cost 的 6862 条任务变更应用到调度，用当前 lp_core+份额
口径重算：LNS 调度 1,833.7M vs 起始调度（同口径）1,886.3M——LNS 在自身口径内确有改进
（1879.2 vs 1886.3，−0.4%）；但当前论文贪心 λ 点在 lp_core 口径是 1,440.1M，比 LNS 调度低 27%。
根因：v2 能量系统用 anchor 消纳锚定，当前管线用 share(r,时段) 基准标定——两套电力层口径不可
通约，LNS 的 0.4% 改进远小于 27% 的口径差。强行把 LNS 调度接入当前 Q3/Q4 冻结链等于制造新的
"数字-代码脱节"。

决策：LNS 升级不采纳，当前 88-90 分全绿交付态保留。LNS 全部产物与可复现补丁留在 v2 目录
（a1b_*.csv、lns_single.log、patch_knee.py、q2_lns_a1b_multiobjective.py 的 BASE/TIME_LIMIT/
GAP/OPERATORS/knee 五处适配），不进主交付链。主 work_dir 未被触碰（TECHNICAL_PASS、哈希
MATCH、快照 34 文件完好）。评审"聚合近似"扣分项如实保留在论文 7.2 局限段。

## 09-03 LNS 终局详文（run_lns_share.py，share 口径直跑）

1. **status=4 根因**（前 agent 遗留 bug）：`milp(options={"threads": 1})` — scipy 1.15 对未知
   option 键 verbatim 透传 HiGHS，触发 "Status 0: Not Set"（status=4）静默失败，0.0s 返回。
   实验矩阵逐项验证：带 threads → status 4；去掉 → status 0 正常求解。已在 run_lns_share.py
   修复并留注释。诊断路径：玩具MILP预检（同进程正常）→ 输入逐字段打印（c/A/integrality 全干净）
   → options 实验矩阵（唯 threads 复现）。诊断探针 probe_milp.py 留档。

2. **share 口径 LNS 实测结果**（threads 修复后）：基线 1,449,337,102 CNY（当前贪心调度，
   GPU 违反 2 格）→ LNS 后 1,450,393,813 CNY（**−0.07% 负优化**），GPU 违反恶化到 242 格。
   根因：MILP 目标只用电价代理成本，无 GPU 容量约束（1000 行等式仅"每任务选一区"），解天然
   倾向堆积低价区，破坏容量可行。v2 版之所以有 GPU 行，是因为它的 MILP 带 GPU 约束行。

3. **路线终局**：LNS/MILP 在两个口径下双重证伪——v2 anchor 口径 −0.4%（真实但被 27% 口径差
   吞没）、share 口径 −0.07% 负优化+破坏可行性。当前贪心 λ 点 1,440.1M 已接近 share 口径
   实际可达最优。93-95 分路线的"任务级 LNS/MILP"在算力预算内无正向收益，正式终结。
   论文维持 88-90 分全绿交付态（TECHNICAL_PASS 12/12、五件哈希 MATCH、主 work_dir 未被触碰）。

4. **遗留资产**：run_lns_share.py（修净版，threads 注释）、lns_share_result.json（终局结论）、
   probe_milp.py（诊断探针）、v2 目录全套 LNS 产物与五处适配补丁。全部可复现，均不进主交付链。

## 09-03 优化轮详文（三向提分）

1. **Q1 基线对照**：从冻结备份提取原始 GBR cell 时发现其属早期超参版本（复现 58.87≠论文 54.99），
   遂放弃重训复现路线，改为"冻结 CSV 为权威 + 朴素基线单测"：季节性朴素（s[h-24]）在 432 点终测集
   RMSE=72.8628、R²=0.1284；GBR pooled 54.9878（由 ques1_prediction_metrics.csv 18 组等权合并精确
   复算验证）相对降低 24.53%、17/18 组占优。对照 cell 以 subprocess 真实执行（6.6s）写入 notebook
   （第 5 个 code cell），补齐附录 Q1 代码缺口并提供跨模态生成源；ques1_benchmark_comparison.csv
   受控登记进 frozen sources。
2. **6.1.1 OAT 重写**：原正文声称"±40% 步长 10% 扫描 81 组合"与实际数据（6 次 OAT 重解）不符——
   借加表之机修正为真实口径，新增表6弹性汇总：电价 1.00（严格同比）、碳强度 0.46（注明其测算内嵌
   碳价 500、基准 2.410e9 与其余 1.308e9 不同）、新能源 0.00（消纳受份额上限约束，可用量增加仅
   扩大弃电空间）。两段陈旧子面板图评（图6.1(b)/6.2(a) 等不存在的子图）改写为与实际图一致。
3. **结构清理**：表号 1-8 连续重排（原三个"表1"+跳号）；空"## 6.2 模型评价"删除；6.3 题意解释
   风险与待复算项并入 7.2 第四点（内容零丢失）；关键词"碳感知调度"（非标准词库词，cross_modal
   WARN）→"时间序列预测"；fix_support_caption.py 表题硬编码同步改表8 防回归。
4. **页数拉锯**：新增文字一度把正文顶到 31 页，压缩 7.2/7.3/5.1/图2段/λ段共约 1700 字后回到 30。
   终态：PDF 74 页、九步全绿（含 cross_modal PASS）、五件哈希 MATCH。

## 2026-09-03 Agent Docker 路线图 LP 闭环复核（task 20260903-094022-fdd4911527b6756461f209c8a4aa0c5a）

- 续传终态：`completed`，`workflow_state=frozen`，累计 42 次 chat、760221 token，`artifact_set_id=1103b6cf...8565`；最终验收为 `TECHNICAL_PASS`，其中 preflight=`CONDITIONAL_PASS`、submission audit=`WARN`，人工复核仍为 PENDING。
- 隔离干净重跑：使用 `backend/.venv/Scripts/python.exe` 启动新 ipykernel，在系统临时目录清空历史输出后顺序执行 notebook 7 个 code cell；`ques1_results.csv`、`ques2_results.csv`、`ques2_sensitivity.csv` 与任务目录全部 MATCH。独立有理数顶点枚举：基线 `(40,20), Z=2200`；机器 110h 后连续解 `(140/3,50/3), Z=7100/3`，增量 `500/3`；整数解为 `(47,16), Z=2360`；对偶价 `(50/3,20/3)`。候选 manifest 5 个主产物哈希全部 MATCH。SciPy 官方文档核实 HiGHS 默认变量连续，整数约束须显式设置 integrality。
- PDF 实际 50 页。人工抽查页 1/2/9/10：摘要后残留生成用自检清单并独占第 2 页；正文 5.2 把原最优点误写为 `(33.33,33.33)`，与冻结值 `(40,20)` 矛盾；`16.67×10` 被写成 `166.70`，精确值应为 `166.666...`；PDF 表格中 LaTeX 计算口径发生断行/源码样式泄露。当前产物技术可交付但论文六维评审应判 `NEEDS_REVISION`，不可标正式提交。
- 后端修复：失败的非正式 sensitivity 阶段不阻断正式 quesN，但失败 flag 原样留存且移除其图/数值再交 Writer；final acceptance 仅显式 default 可接受条件态并保留 warning，其他 profile 及缺失/未知 profile 严格 PASS；paper_review 材料包明确返回三版本，POST 写入前逐项校验，缺失/变化均 stale，拒绝静默补当前版本。
- 验证：定向 52 tests OK；ruff All checks passed；`docker compose config -q` exit 0。说明同步：STARTUP、PDF 导出说明、最终人工复核清单、AGENT_MEMORY；无需改 CUMCM 模板替换指南及 export_profiles README（未改模板/资源结构）。

## 2026-09-04 PR #40 合并与 CI 转绿

- feat/agent-docker-backend-roadmap 经 f64224a（P2 边界：sensitivity 失败隔离、default 条件放行限定、paper_review 三版本写前校验）与 f501077（finalize 首轮失败清理新 candidate_manifest 的假发布窗口；guardrail/guidance/pdf huashubei 11pt/1.3/writer 措辞/直调 modeling() 补 idempotency_key 等旧断言同步批 A 契约）后，PR #40 以 merge commit 68cb89d 合并进 main。
- 分支 CI 此前持续失败（5 failures+3 errors，均为主目录未提交的批 A/B/C 契约更新未同步旧测试所致）；本地复现并修复后，CI 全量 `unittest discover app/tests` 905 项 OK（skipped=11），ruff 通过。
- 实测评审闭环：LP 20260903-094022 评审 NEEDS_REVISION 已按新契约落盘（`_stale:false`）；故意提交错误 manuscript_sha256 返回 422 且 paper_review.json 哈希未变。

## 2026-09-04 门禁加固（PR #41，commit be84a99）

- 动机：v23/A 稿两次"方法声明与代码脱节"逃逸 + 09-03 返修轮 `$ \sum` 开界吞字出 □ + q4 缓存键只含情景参数差点复用旧值。
- `ALGORITHM_CLAIMS` 从 3 项扩到 8 项（+ε-约束/LNS/MILP/NSGA/模拟退火）；ε-约束证据不再接受裸 eps 子串（baseline×eps 加权标量化必须被拒），用 `addConstr/<=/>=` 60 字符内含 eps/epsilon 的约束式上下文匹配；前瞻与否定豁免词表改为与声明词表共用 `METHOD_CLAIM_ALTERNATION`，避免扩面后漏豁免。
- `code_text_parity` 新增 `find_literal_result_writes`：结果/验收 CSV 的 `pd.DataFrame(纯字面量)` 直接写盘 → 记入 `literal_result_writes` 并整体降 WARN。
- preflight 新增 `math_dollar_spacing`（conditional）：剥离 $$ 展示式后行内 `$ ` 开界且同行存在闭合 $ 即报；pdf_visual 新增 `missing_glyphs`（硬门禁）：全页文本层扫 U+FFFD/U+25A1/NUL，附页码、计数与 ±40 字符样本。
- Coder 提示词 EXECUTION-BUDGET 段新增强制规则：持久化缓存键必须包含影响输出的输入文件与参数 SHA-256，禁止仅情景名/循环索引。
- 验证：新测试 `test_gate_hardening.py` 16 项；全量 discover 921 项 OK（skipped=2）；ruff 通过；PR CI Backend pass。

## 09-03 评审二轮修复详文（外审 89-91，中心 90）

评审五项正文矛盾全部修复：①表3"Pareto前沿点数5.0"→"候选集内非支配代表点数2.0"（与验收CSV/
frozen/支配检验三方对齐）；②删除"所有cost低于基线者均未通过约束检验、基线为最低成本可认证解"
旧审计文（与λ解1.44B<1.565B直接矛盾），改写为如实总结（λ=0降8.0%、6候选留2非支配）；③压力题
模板污染6处（压力目标/波动上限/稳定判据/mm³·MPa/超压工况/执行器动态响应/峰值压力/质量流量守恒）
→电力域表述；④虚MILP/LP松弛取整3处（2.4/5.1方法段/5.4）→真实分层法（聚合贪心+LP对偶定价，
Q4固定迁移再优化）；⑤图12成本区间去Wilson改"成本均值95%正态近似置信区间"；⑥凸性声明收紧
（电力层凸+任务层启发式近似，不保证完整Pareto前沿）。

**额外发现（评审未抓到的同类问题）**：论文声称"12组网格以验证集RMSE为唯一准则选定(200,4,0.1)"，
实测18组网格验证集最优是(100,5,0.1)（val 44.5486），(200,4,0.1)仅列第10——选型声明不实。处置：
按协议真跑18组搜索→重训→终测，新数字 RMSE 54.7213/R² 0.5084（优于旧值）、朴素对照改进24.9%、
17/18组占优不变；完整协议cell（含搜索日志stdout）替换notebook附录旧Q1 cell，同时闭合评审第8条
（附录此前无GBR训练代码）。级联更新：摘要/1.3协议段/5.1/表2/7.2数字、frozen metrics两条、
4个源哈希+资产清单3项受控重绑。

终态：PDF 77页（附录+3页协议代码）、正文30页、九步全绿（含cross_modal PASS）、五件哈希MATCH、
全文缺字形0页、旧数字/旧声明零残留。预期评分：评审五项+选型声明修正+附录Q1闭合 → 91-92 档。

## 09-03 评审三轮收尾（外审定档 91，目标 91-92）

四处纯文字清理（评审确认零模型改动）：①"负压力"漏网→SOC越界/功率平衡/购售电边界表述；
②"ε放松后回流东部"→"λ_c降低时部分任务回流东部"；③前沿用词统一："5个代表方案构成主要前沿"→
"5个候选方案经支配检验2个构成非支配前沿"，图1注去"五点沿前沿"；④6.1.3 去"Wilson半宽占成本
4.57%"误称（改成本均值正态近似CI）、"未陷局部最优"→"未观察到明显支配反转"、"稳健最优选择"→
"候选集与扰动范围内表现稳健"。

页数拉回：清理净增~70字把正文顶回31页；先砍85字冗余，再把17条"如图N所示…展示了本节相关计算
结果"独立lead句并入图注段（9处补"图N："前缀、8处图注自带图号），figure_references 全保，
正文回到30页。终态：PDF 76页、九步全绿、五件哈希MATCH、缺字形0、旧术语零残留。

后续：论文整改建议尽快从 feat/agent-docker-backend-roadmap 摘出（cherry-pick 论文相关文件到
main 或专分支）后提交；模型/CSV/图冻结不再触碰。

## 09-03 评审四轮收尾（定档 92，可冻结）

评审人工终检发现四处图号语义错误（figure_references 门禁只查存在性不查正确性——门禁盲区实证）：
①"图5：图3为…"/"图6：图4为…"双编号（合并lead句时把旧章节号段落也加了前缀）；②图7填充句+SOC
说明错标图8、功率调度说明无号；③"图16：图6.3给出"双编号；④"图6.4直方图"旧章节号→图17。另修
Q2区旧编号"图2检验/见图3印证"→图4/图5。全部按顺序图号统一，12项PDF语义验证通过。

Q1调度证据链闭合：评审指出附录只读 ques1_schedule_result.csv 无生成代码。从冻结备份提取原调度器
cell（成本最小化贪心，导入linprog但未调用——顺带暴露正文"构建线性规划模型求解最优跨区调配"为过度
声明），scratch 重跑验证两 CSV dataframe 级完全一致（哈希差异仅浮点格式），以真实 stdout 插入
notebook（lp_core 之后、协议 cell 之前）。正文三处 LP 声明改贪心口径（摘要/5.1方法/5.1结果），
solver_status 表述改"求解流程状态码（全部任务完成分配与约束校验）"。Q3 目标函数句去"与碳排放"
（公式只有电费，碳为结果指标）。

终态：PDF 79 页（附录+3页调度器代码）、正文 30 页、九步全绿、五件哈希 MATCH、缺字形 0。
评审确认：模型/CSV/图/frozen 自此冻结；提交走独立 paper 分支，与 roadmap 分支分开。

## 09-04 符号渲染专题（人工审核发现"未对齐符号与乱码"）

三类根因全部修复并终验（缺字形 0）：
1. **表头 BOM**：表4 第一列 `| \ufeff指标ID |` 导致列错位——全文清除 U+FEFF。
2. **正文裸符号**：`1.96s/(√n·C̄)`（组合 macron 悬浮）→ LaTeX 数学；`SOC终态≥初态`→中文；
   `10⁻¹³`/`10⁹`→数学模式；`tCO₂`/`kgCO₂`→`$_2$`；`∈{...}`×4→"取…/…"或数学模式。
   注意：一次批量脚本因 assert 中断未落盘，教训是**批量替换脚本必须逐项容错或先全部校验再写**。
3. **附录代码符号**：pandoc `--listings` 下 lstset literate 对 λ/≤ 实测不生效（→×–² 靠字体回退
   存活），17 处渲染成 U+FFFF。放弃 literate，改将 notebook 6 个代码格与 4 个 audit 脚本中的
   λ≤≥≠≈±↑↓×²→ ASCII 化（lambda/<=/>=/!=/~/=+/-/(升)(降)/x/^2/->），仅涉注释与 print 字符串、
   零逻辑变化；cell==file 逐字节复验通过；附录重建、notebook 哈希受控重绑。

终态：PDF 79 页（正文 30）、九步全绿、TECHNICAL_PASS 12/12、五件哈希 MATCH、
U+FFFF/U+FFFD/□ 全文 0。

## 09-04 轮出：Agent 调用 Docker 后端路线图（PR #40 已合并，主文件轮出）


- **目标**：外层 Agent 稳定调用本机 Docker 后端（`docker compose up` 仅 backend+redis，前端 `--profile frontend` 可选），0.0.15 规则进入实际执行链而非仅 `skills/`。
- **批 A**：`writer 600-900字4段 / 11pt1.3 / 30页+摘要单页` 收敛至 profile；手机号漏检等通用修复；`docker-compose.yml` 去 `5173/api` 耦合、`frontend` 加 profile、`common_router` 增 `deployment`、`task_client` 基址切 `8000`。
- **批 B**：`task_client.py` 薄客户端（`doctor/submit/inspect/events/guide/approve-model/revise-model/review-results/resume/cancel/artifacts/repair-*`）+ 后端 `Idempotency-Key/GET /tasks/{id}/events/artifacts` 与 `guidance_id` 回执（`accepted/consumed`）。
- **批 C**：`task_budget.json` 累计预算（调用/known token/运行时长）持久化、未知 usage 标 `unknown`、`_finalize` 备份保留+`to_thread` 防阻塞、`cancel` 保留停止证据。
- **批 D**：`backend/app/resources/modeling_guides/{01-05}.md`（确定性基线/硬约束/统计严谨/Pareto/溯源）按诊断 profile 加载；`paper_review` 六维评审材料包+版本绑定（`manuscript_sha256/frozen_result_id/artifact_set_id`）+ 五类分流。
- **批 E**：`figure_plan` 路由（data/template/diagram/physical）+ 多面板+追溯校验；`doctor` 容器体检与模板能力表（`huawei→huaweibei` 别名，后端仅 4 profile）。
- **批 F**：`test_agent_docker_backend.py` 10 项契约验收 + 轻量 LP 烟雾题 + 完整链路哈希核对；`STARTUP.md` 增 Agent 调用手册。
- 取证：`mathmodel_workspace_0.0.15 (323 MB)`；验证：`task_client doctor` + `docker compose config` + `unittest` 10/10。

## 09-04 Q1 调度器 v3（评审实锤 v1 未执行约束 → 真约束真结果）

评审对 79 页版核查附录调度源码后实锤：v1 读了 Available_GPU 不用、时窗只查开工不查完工、
`min(...,2406)` 截断使违反检查恒 0、price+carbon 量纲错误、ms×0.01 无货币化定义、完成率恒 1.0。

v3 重写（q1_sched_v2.py）：EDF 处理序（slack 升序）→ 时延可行区域内枚举开工 → 完整持续时间
End=s+⌈d⌉≤min(lf,2406)（实时按题面=到达即开工+2406 结清，lf 列仅自然完成时刻）→ 逐小时按
重叠占比 θ 折算占用并检查 usage+GPU·θ≤Available_GPU → 纯电费成本 Σθ·P·PUE·Price（CNY 量纲
一致）、同成本以时延为次级键 → 调度后从最终 schedule 独立复算 v_GPU/v_deadline/v_horizon。
结果：538/538 完成、三项复算全 0、迁移比例 0.708、峰值利用率 100%（实例总需求≈容量 90%，
EDF 顺序是完成率保住的关键）。预测/对照 CSV 字节不变（确定性佐证）。

事故与恢复：批量替换脚本的 find 边界串在 2.1 与 5.1 两处出现，误删 121 行中段（2.2~5.1）。
以 bak_semantic_fix 拼接 + 重放四轮/符号/三轮编辑恢复并复核全部标题与残留。教训已写入主记忆：
替换前必须校验边界唯一性。新调度器文件注释含 ≈ 漏过 ASCII 化，补清。

终态：PDF 82 页（附录含 v3 全码）、正文 30、九步全绿、TECHNICAL_PASS 12/12、五件哈希 MATCH、
缺字形 0、正文旧数字/旧声明零残留。评审冻结条件（真约束下完成率 1.0 + 违反 0）已满足。

## 09-04 回归清理（拼接副作用二次 scrub，7 处）

误删恢复用的 bak 是精修轮前状态，把 2.2（ε-约束+LP松弛取整）、2.4（MILP框架+取整修复+Wilson
达标准入）、四（压力目标模板污染+问题四MILP段）、5.4.2/6.1.2（Wilson 准入措辞）旧文带回。
本轮全量回归扫描（29 个旧短语探针）逐处修净：2.2→碳价λ扫描+聚合贪心+对偶定价；2.4→聚合块
贪心+电力LP；四→电价/碳强度/新能源/储能参数；问题四段→分层方案表述；Wilson 三处"准入约束"
→"评估QoS合规率统计可靠性/以置信下限为参照"。终验 7 项语义检查全过、缺字形 0、九步全绿
（81 页正文 29）。教训升级：从旧备份恢复后必须跑全量旧短语扫描，不能只补记得的编辑。


## 2026-09-04 论文终版收口与链修复（新门禁首次实战拦截）

- 触发：部署 #41/#43/#45 后对终版论文重发证据，新 missing_glyphs 门（含 NUL）在 16:52 批准版 PDF 第 9 页逮住 4 个 0x00——来自旧版断界公式，会话此前只扫 U+FFFF/U+FFFD/□ 未覆盖 NUL；论文终版 res.md 已修该公式，重渲染即消除。
- 链状态诊断：16:52 五件套绑定 md@e93b0eb；论文会话随后又改 res.md(修公式/改名/B.1同步)、notebook(17:50 v3 单元)、frozen_results.json 与 ques1_acceptance_metrics.csv(17:02)，未重发链 → manifest hash_mismatch、asset 来源哈希脱钩、complete_source_appendix 双失配。会话终版 res.md 与 notebook 的 B.1 漂移为标签一行（求解器状态→调度验收状态）+SHA 行。
- 处置（internal/audit_20260904/）：final_sync_regate.py 以当前 notebook 整块重嵌 B.1+SHA、重建资产清单、本机 huashubei 路线导出；发现 profile 路线排版(62页)与批准版(81页)不一致后，改用会话自己的 regen_pdf.py 排版路线(12pt/lst_header)渲染终版 md=85页正文30；final_tail.py 重发 visual/manifest/audit/final。任务级路线教训：容器 export_markdown_to_pdf 与本机 regen_pdf 排版参数不同，重导必须沿用该任务批准的路线。
- 终态：pdf_visual PASS(85页/正文30/missing_glyphs 0，独立复扫 NUL/U+FFFD/U+FFFF/U+FFFE/□ 全 0)、preflight PASS、cross_modal PASS、submission_audit PASS、final_acceptance TECHNICAL_PASS 12/12、manifest 五件 MATCH、res.md 与 notebook 逐字一致。期间容器 ensure_paper_assets_manifest 会同步 B.1 哈希行(17866793…)——副作用已知、与终态一致。
- 论文会话最后编辑 17:50-17:52（互斥核验段与 B.1 v3 内容），18:09 后盘上安静；本轮所有报告基于该终版内容。

## 09-04 互斥约束独立核验（评审第4项，冻结前最后一块）

LP 解存在同时充放电（12.3% 区域·小时、max 260MW）——源于题面"新能源充电计入利用率"机制的理性流。
按评审指引不隐瞒：在电力层模型追加二进制互斥（ch≤MaxCharge·u、dis≤MaxDischarge·(1−u)）以 MILP
重解六区域（time_limit 900s、gap 1e-4）。结果：合计成本 +0.79%（A/B/C +0.4%、D +6.3% 且其 gap
1.3% 为上界、E/F −0.1~−0.2%），充放电总吞吐逐区域完全不变，2.37亿节约在互斥口径下约 2.27亿、
方向量级不翻。主模型保留 LP 形式，5.3.2 如实报告，明细 ques3_exclusion_check.csv（notebook 第7格
真实执行 stdout、frozen 登记、资产清单同步）。四处文字修复（图1双面板虚构/图2储能调节跨题/
非凸→LP 严谨化/表2"调度验收状态"）同步 CSV/协议cell/notebook/frozen 四层标签。

终态：PDF 85 页、正文 30、九步全绿、缺字形 0、五件哈希 MATCH。评审五项收尾全部闭环。

## 09-04 评审五轮收尾（定档 91 → 冲 92-93）

四项闭环：①Q2→Q4 接口：λ=500 从"推荐方案"改为"预先指定的中等碳价参考方案（控制变量输入）"，
正文明示问题二非支配方案为 λ=0/1500、λ=500 非 Pareto 推荐——采纳评审方案 B（不重跑）；
②MC 第三随机变量 γ(碳强度)~U(0.8,1.2) 补入摘要与 6.1.2（代码本就三参数扰动）；
③ques4_acceptance_metrics.csv 的 montecarlo_ci_width 标签去 Wilson 误称（改"成本均值95%正态
近似置信区间相对半宽"，值 0.045684 不变），frozen label 同步；Wilson 图 errorbar 上侧误差
sqh-sql→sqh-sqp 修复并重出图；④Q3 最后一句"原始非凸→LP"旧措辞清除。

q4_model 缓存重跑 339s：仅 2 个文件变化（acceptance CSV 标签 + wilson png），其余 12 情景/MC/
OAT 产物字节级一致（缓存确定性再证）。附录注释又引入 λ 踩缺字形一次（新门禁 missing_glyphs
当场拦下）——规则扩展：附录代码注释同样只准 ASCII 符号。

终态：85 页、正文 30、九步全绿（含 PR41-45 加固门禁）、缺字形 0、五件哈希 MATCH。

## 09-04 评审六轮（阻断项清除）

两处实质问题：①5.4.1 仍描述"跨区域任务迁移量 M 为决策变量 + 六目标加权 ε-约束"——与下一页
"固定迁移方案只重解电力层"自相矛盾。整段重写为真实口径：优化变量=电力层（购/售/充/放/SOC/
新能源吸收），迁移由问题二参考方案 λ_c=500 固定；六指标报告、三维（成本/碳排/利用率）非支配、
峰值购电为安全裕度指标。符号表 M 行加注"由问题二确定并固定，非问题四决策变量"。
②Wilson CI 来源澄清：是对固定迁移方案任务时延合规率的 Bernoulli 置信估计，非 100 次 MC 样本
区间（MC 只扰动电力侧三参数、不重优化迁移，QoS 跨样本恒定）——正文如实写明。
连带：Q2"线性规划松弛配合贪心指派"句改真实分层表述；表3"求解器状态码"→"求解流程验收状态"
（正文+CSV+q2_model+notebook+frozen 五层同步）。

终态：86 页、正文 30、九步全绿、7 项语义验证过、缺字形 0、五件哈希 MATCH。评审预期定档 91.5-92.5。

## 09-04 评审七轮收尾（定档 92，可冻结）

三处终清全部落地：①MC 的 95% CI 统计学解释修正——W=1.96s/(√n·C̄) 是均值估计的相对置信半宽，
不是单情景成本覆盖区间；删除"绝大多数场景落在 C̄±W 范围内"错误表述，单次波动改由样本标准差与
极差比 0.8025 刻画；6.1.3"正常波动下误差可控"→"成本均值的估计误差可控"。②附录 q4_model 两处
过时注释（"Q2 的 lambda 是迁移打分权重而非碳价"）更新为层级差异表述（λ_c 是迁移层边际评分的碳价
权重，与 Q4 购电目标 carbon_price 层级不同，不作数值等价比较）。③Q4 的 12 情景点术语从"Pareto
前沿/帕累托前沿"改为"非支配情景结果集"（不同外生参数情景，非同可行域决策权衡），图10 题注与
alt 同步。

终态：86 页、正文 30、九步全绿、缺字形 0、五件哈希 MATCH。评审定档 92 分、可冻结。
注意：跨页句子的 PDF 子串检查会被页码截断产生假阴性（"不代表单个情景成本的覆盖区间"跨 P27/28），
语义验证应逐页或归一化后比对。

## 09-04 评审八轮收尾（定档 92.5，可冻结）

两项验证语义修正（评审认定的最后阻断项）：
1. **跨问一致性检查对象修正**：Q4 沿用 λ=500 迁移方案，但 baseline_cost_consistency 却拿 Q4
   基准情景成本对比 Q2 的 lam0——实测偏差 6.05e-5 恰等于 (lam500−lam0)/lam0，证明它测的是碳价
   效应而非实现一致性。改为对比 lam500 后 bc_dev=0.000000（机器精度），证实两问同迁移方案、
   同电力层 LP、同标定负荷实现严格一致。q4_model 相关注释同步改写。
2. **图12(b) 统计学表述**："绝大多数样本落入置信带内"误把均值 CI 当样本覆盖带——改为"橙色区域
   表示成本均值的95%正态近似置信区间，刻画均值估计不确定性而非单个样本覆盖区间；n=100 下相对
   半宽 4.57% 达到预设精度"。

连带：新写注释含 λ 被 missing_glyphs 加固门禁拦下（PR#43 首战建功），全 audit 脚本+notebook
补扫 ASCII 化（q4_model λ、q1_benchmark ²）；并行会话 17:55 的 PDF 导出被截断（62 页，停在附录
1383 行），重导恢复（85→86 页）。

终态：86 页（正文 30 + 附录含 v3 调度器/协议/互斥检查全码）、九步全绿、TECHNICAL_PASS 12/12、
五件哈希 MATCH、bc_dev=0.0、缺字形 0。评审冻结条件满足。

## 2026-09-05 评审九轮收口（定档92.5冻结·两处文字/注释终清）

评审定档 92.5 并明确"可以冻结"，仅留两处可选残留，本轮执行并全链收敛：

1. **附录 Q4 旧注释**：顶部 docstring 第4条尾巴"纯成本口径参照 lambda=0"与节头"应与 Q2 的
   纯成本口径吻合"均与实际代码（q2_lam500 对比）打架。改为"同口径参照点为 Q2 的 lambda=500
   参考迁移方案"。q4_model.py 与 notebook cell#11 同步（cell==file 逐字节校验），并以
   jupyter_client 真实重跑该格（631s；内核显式钉 venv python——默认 python3 kernelspec 的
   argv[0] 是裸 "python"，不显式钉会依赖 PATH 解析）。
2. **5.2.2 通用四权重公式删除**：min Σw_k·f_k 与真实实现（min C+λ_c E）不符，删公式并把
   前导冒号收句；真实公式本在同段内联保留。

确定性证明：重跑前后全部 ques*.csv SHA-256 逐字节一致（seed=42），stdout 复证
bc_dev=0.00e+00、非支配=6、场景范围=0.7408、残差=2.3e-13、MC=100、CI半宽=0.0457。

### 重绑副作用修复（本轮新踩坑，后续重绑必查）

- `rebind_manifest.py` → `write_frozen_results_from_execution_validation` 会 os.walk work_dir
  把 6 个旧 LNS 路线脚本回填 `executed_code_sources`，而 `collect_code_sources` 以其为附录
  白名单 → `fix_appendix` 把已证伪的 LNS 脚本全嵌进附录（104k→393k 字符）。修复：writer
  重生成后 post-patch 收窄回 ["notebook.ipynb"]（frozen_results 与
  reproducibility_manifest.entrypoints 两处），重跑 fix_appendix 收缩回 notebook 单源。
  该收窄是上一轮既有人为终态，**每次重绑后必须补做**。
- ques2 约束 `pareto_point_count`：rebind 按当前 CSV 刷 actual=2.0（诚实非支配数），暴露
  遗留 target=5.0（旧"5λ候选数"显示语义）→ feasible=False。代码声明的达标规则是 >=2
  （非空非支配前沿，q2_model.py 达标列），对齐 target=2.0 后须用门禁自身 `_check_constraint`
  重算 feasible——writer 只透传 manifest 的 feasible 字段，改 target 不会自动重算。
- `reconcile_gates2.py` 必须以 backend 为 CWD 启动（WORK_DIR_ROOT="project/work_dir" 是相对
  路径，从别处启动会拼出翻倍路径直接 FileNotFoundError）。
- support_materials.zip 内含 notebook.ipynb，submission_audit 逐条核对 zip 内 SHA-256 →
  notebook 变更后必须 `write_support_materials_manifest` 重打包。
- `fix_support_caption.py` 磁盘版已是"表8 支撑材料文件列表"（正文表1-7+附录表8），编号正确。

### 终态

86 页（正文 30）、九步全绿（docx/pdf 哈希 MATCH、execution_validation PASS、preflight PASS、
cross_modal PASS、candidate manifest MATCH、submission_audit PASS、TECHNICAL_PASS 12/12）、
缺字形 0、bc_dev=0.0、旧短语与四权重公式全文零残留。新脚本：rerun_q4_comment.py、
fix_rebind_fallout.py（留 internal/audit_20260901/）。

### 评审终局（同日凌晨补充）

评审终定档 **92.5-93（中心约 92.8）**，宣布"可以冻结、不再深挖审计"（继续审计边际收益低且易引入回归）。最后一句纯文字修正：5.2.3 首句"求解器以状态码1.0正常终止"→"求解流程验收状态码为 1.0"（与表3"求解流程验收状态"及 5.2.3 后文口径一致；全文仅此一处）。仅改 res.md 一句 → regen_pdf（3,666,692 B）→ reconcile_gates2 九步重收敛全绿（TECHNICAL_PASS 12/12、86 页、缺字形 0）。模型/CSV/参数/图数据/frozen 自此冻结不再触碰；剩余全为人工侧事项。

## 2026-09-05 主文件轮转：v23 节原文移入（09-02 落地条目全文）

## v23 算电协同·去伪造重建（2026-09-02 落地，task=20260830-234433-4b3226317d54a94062be7a3379cf1a10）

- **真值与血统（已被 09-02~09-03 多轮更新覆盖）**：notebook 4 code cell 全真实执行；Q4 曾因
  `scenario_objective_range` 方向写反误报，修后 100 样本真实达标；frozen/manifest 走
  rebind→hash 重算受控刷新。当前冻结数字以 09-03 终态为准（MC 均值 1,284,013,383）。
- **Q4 争议已消解（09-02 17:00 轮）**：原 Q4 失败是 `scenario_objective_range` **约束方向写
  反**（写成 `lte 0.5`，实为 `gte 0.05`），不是模型不行。修 `lp_core.py`/`q4_model.py` 后按
  **100 样本真实重算**（q4_rerun5）：非支配 6≥5、CI 0.0457≤0.05、一致性 6.1e-05≤0.01、
  区分度 0.7408≥0.05、MC 成本均值 1,284,013,383 —— **四个目标全部真实达标，无需放宽**。
  res.md 由 `align_resmd_q4_v2.py` 同步到 100 样本数值（0.0597→0.0457 等）。
- **全门禁已绿（09-02 19:44）**：`final_acceptance=TECHNICAL_PASS`（12/12 项通过）、
  `submission_audit=PASS`（14/14）、`preflight=PASS`、`pdf_visual=PASS`、`cross_modal=PASS`、
  `execution_validation_report=PASS`。res.pdf 68 页（正文 30 ≤ 上限 30，附录 27 页为完整源码）、
  res.docx 2.79MB。三项原 FAIL（`submission_audit` 陈旧 / `artifact_freshness` /
  `complete_source_appendix`）已清零。
- 复用脚本：`internal/audit_20260901/{q4_model.py, lp_core.py, patch_q4_cell.py,
  rebind_manifest.py, run_gates.py, draw_q4_ci.py, align_resmd_q4_v2.py, trim_abstract.py,
  compress_resmd2.py, regen_pdf.py, **fix_appendix.py, fix_support_caption.py,
  reconcile_gates2.py**}`。复用原则：任务级脚本只放 work_dir 内部审计子目录（见 AGENTS.md
  工作区防污染铁律）。详细过程见 `.workbuddy-ai/memory/2026-09-02.md`。
- [09-02 第五~七轮+09-03 精修轮] P0-P3收敛、摘要5段、提示词/编辑政策治本；审计修复（11孤立
  引用删、9悬空内嵌、Mavrotas{[6]}→[9]、6.2→6.3、AI审计24.5→约21）；外审86-89→四项语义对齐
  （Q2统一λ扫描+对偶定价+真实支配检验{lam0,lam1500}、Q3披露循环约束1.5次/日+敏感性、Q4降格
  固定迁移再优化、CI拆分±4.57%、关键词五项、7.2真实局限）。终态PDF 72页正文30、TECHNICAL_PASS
  12/12、五件哈希MATCH。陷阱：append_code_appendix 必跟 fix_support_caption.py。
  详文 docs/memory/2026-09.md。

## 2026-09-05 框架收口：#48 三查部署与 v23 终版证据重发

- PR #48（044d549/a1fb564，隔离 worktree 完成）：章节式图引用假阴性、表号连续性（1..N 缺/跳/重）、
  artifact_freshness 增 mtime 哨兵（res.md/notebook/frozen/结果CSV 任一晚于导出链基准即 FAIL，2s 容差）。
  动因：09-04 晚 v23 六次发链后源回改不重发，哈希核对全部漏网（CSV/notebook 从不进哈希）。
- 部署：主检出切 main 并 pull 至 a1fb564，backend 容器重启，容器内实证 #48 代码在位。
- 09-05 deploy_regate.py（internal/audit_20260905/）对论文会话 11:10 终版链（86页正文30）用 #41-#48
  全量门禁重发报告：pdf_visual/preflight/cross_modal/submission_audit 全 PASS、final_acceptance
  TECHNICAL_PASS 12/12、图引用 17/17 无章节式混用、表号连续、mtime 哨兵零报（链一致）、
  五件哈希 MATCH、交付物字节未动。v23 自此处于新门禁全背书的冻结候选态。
- 记忆治理：09-04~09-05 两会话共五次双向分叉，均以 union+轮转收口（#44/#46/#47/本次）；
  归档中三处历史控制字符（\x08×2、\x00×1，heredoc 转义遗留）由论文会话 round3 重构时自愈为
  文字表述。教训：跨会话写记忆必须经 git PR，工作区直写只作为对端暂存。

## 2026-09-05 稳定版本工程（PR #51，main=4ab9d1b，批次0-5）

- 触发：外部审计定档"个人可用但非生产级"，五缺口全部对盘实锤（空源审批 PASS、res.json 语义
  滞后、completed+quality_repair 矛盾、PENDING_HUMAN_REVIEW、git_commit=null）。
- 批次1 审批绑定：教训=登记约定 quesN_result(s).csv 与 v23 描述性命名（ques1_schedule_result.csv
  等 33 个）全不匹配 → 扫描器改绑 execution_validation.json 的 constraints/metrics/figures 登记；
  三态分离后 08-30 由"0源PASS"变 16 源真 PASS，08-17 源漂移正确阻断（旧审批不可迁移）。
- 批次2 内容修订：paper_revision.json 台账（res.json/res.md/frozen 三哈希+origin）；统一保存
  入口=save_result+三类 repair candidate+CLI manual_save；preflight 新增 res_json_sync 硬门，
  校准=干净机器链（含全部归一化重写）零误报、v23 12/12 真阳性（round3 确实全节重写）。
  阈值设计：分节归一化后 <50 码点跳过（标题页等），整节包含失败时按 ≥25 码点句级 90% 命中率兜底。
- 批次3 状态收敛：合法对应表落地为 task_state_diagnosis（CONSISTENT/TRANSITIONAL_EXPORT/
  CONTRADICTION）；08-30 的 completed+*_pending_export 是设计内过渡（候选返修后仅导出），
  08-23 的 completed+quality_repair 才是矛盾。reconcile 强制操作人+理由、写审计、绝不伪造审批。
- 批次4 部署预算：镜像本就 COPY . .，问题只是 compose 挂载覆盖——新增 docker-compose.stable.yml
  去挂载+GIT_COMMIT/BUILD_ID 注入（common_router 已有 env 读取路径，零改动接通）；task_budget
  损坏/task_id 不匹配改为隔离 .corrupt-<ts> + reset_from_corrupt 标记。
- 批次5 历史定性：08-23 res.json 是 deterministic_recovery 包装结构（非分节字典），禁作恢复源；
  08-30 交付件字节未动，res.json 旧方法表述登记为"禁作恢复/重导出内容源"。
- 验证：980 全量 OK、ruff 干净、CI 绿；轻量真实 LP 任务经 task_client 提交（A2 冒烟一并完成）。


## 09-06 算法证据门禁与 LP 导出恢复

- 独立分支 `fix/algorithm-evidence-ledger-v2` 修复 `paper_postprocessor`：算法证据只阻断明确当前采用声明；未来改进、比较/否定、待复算语境排除并记录 `excluded_claims`；算法源码证据复用本轮 `append_code_appendix()` 的源码集合；新增 GA/PSO/MILP/脚注同步回归覆盖。
- `paper_repair_candidate` 与 `task-refresh` 统一接受无硬失败的 `CONDITIONAL_PASS`；不放宽 `severity=fail`。脚注定义搬移与 `[^n]`→`[n]` 后，`res_json_sync` 规范化不再误报。
- LP 任务 `20260906-033157-bd8195551a4a760570d999c09194c6ab`：冻结结果 `frozen_results.json` SHA `ea38a3c6…0ea518`、执行证据 SHA `bb30986d…17b69b5` 未变；候选修复仅以既有 `res.json` 分节重组 `res.md`，任务进入 `paper_repair_pending_export`。
- export-only 重建已生成 `res.docx`、`res.pdf`、`latex_project/main.tex/main.pdf`、manifest/audit；`paper_preflight_report=CONDITIONAL_PASS`，`res_json_sync=PASS`、`algorithm_evidence=PASS`，但 `final_acceptance=TECHNICAL_FAIL`：PDF visual 仍有摘要跨页、附录 `￿` 字形、1 页右边距；队员需人工版式修复/确认。此次不重跑 Provider、Notebook 或数学求解。
- 细节与后续处理见本归档；主记忆仅保留结论锚点。

# 2026-09 会话归档

## 09-02 第五轮：P0-P3 瑕疵修复与门禁再收敛（task=20260830-234433-4b3226317d54a94062be7a3379cf1a10）

**背景**：第四轮收敛到 TECHNICAL_PASS 后，外部审查确认 5 项属实并给出 P0-P3 清单。P0（图注
100 口径三处 + ques4_uncertainty_ci.png 双面板重绘）已由上一轮完成；本轮执行 P1/P2/P3 并再收敛。

**P1 代码同步与真实重跑**
1. 精确 diff 定位：notebook cell2(lp_core) 缺 1444 字符 `calibrate_task_power_scale`（AI 负荷按
   来源区域标定，任务级重建比附件 Baseline 系统性偏高 ~14%）；cell8(q4_model) 差 1864 字符
   （MC_N 50→100、AI 负荷标定、非支配判定从二维 (cost,carbon) 升级为三维 (cost,carbon,utilization)）。
   cell4(q2)/cell6(q3) 与 audit 文件逐字一致。
2. 替换 cell2←lp_core.py、cell8←q4_model.py（备份 internal/audit_20260901/notebook.bak_pre_p1.ipynb）。
3. jupyter_client 8.6.3 + venv python3 kernelspec 手写执行循环（rerun_cells.py），CWD=work_dir，
   每格执行前注入 __file__=audit 文件路径。真实重跑 cell2(42s)/cell6(176s)/cell8(662s，12 情景+100 MC
   缓存全命中)；cell4(Q2, 3481s) 按简报跳过（源码一致、输出与冻结吻合）。
4. **确定性验证**：重跑后 17 项冻结 CSV 源全部字节级一致（仅 notebook.ipynb 哈希合法变化）；
   MC 100 样本 mean=1,284,013,382.64、CI 半宽比独立复算=0.0457，与冻结逐字吻合。

**受控重绑**：frozen_results.json 中 notebook.ipynb 哈希 9017b48b→c3e19dac（唯一变化源），
随后 reconcile_gates2.py 全链重收敛 → 六门禁全绿。

**P2**：cell2 的 ε=0.7 单区域诊断行（cost=226,495,937,439）在 lp_core.py 源码加注释：单区域碳
约束在"消纳份额+进口上限"下不可行，226B 是进口松弛(1e6 CNY/MW)罚信号而非可行解；全局碳约束
由 Q2 迁移层处理。注释随附录入文，消除评审误读面。

**P3**：res.md L458 插入口径脚注：情景区分度 0.7408（12 情景成本极差/均值，sc_range）与 MC 成本
极差比 0.8025（100 样本 (max-min)/mean，scenario_range_ratio）口径不同、互补不替代。插入时
曾引入  控制符（heredoc 转义），已修复为 ar{C}。

**附录与导出**：fix_appendix.py --apply（附录新增标定函数与三维非支配代码，per-cell SHA
97b6126b4a11…）→ fix_support_caption.py 补回"表7"表题（append_code_appendix 每次重建都会
吃掉该表题，已确认为必跟步骤）→ regen_pdf.py 重导 res.pdf **71 页**（正文 30，附录 33；
新增代码 +3 页）→ reconcile_gates2.py（backend CWD）。

**终态核验（独立对盘）**：final_acceptance=TECHNICAL_PASS 12/12；preflight/pdf_visual(71页,
body 30)/submission_audit/cross_modal/execution_validation 全 PASS；res.md/json/docx/pdf/
frozen_results 五件与 candidate_manifest 逐哈希 MATCH；"Monte Carlo (50)"/"50个抽样"/"1.33"
零残留；notebook outputs 含"情景12 MC100"执行痕迹。

**已知边界**：listings 环境缺 λ/≤/≥ 字形（lmmono 警告，附录代码注释装饰性缺字）；human_review
仍 PENDING_HUMAN_REVIEW（四项人工复核未做）；git 工作树未提交（待用户指令）。

## 09-03 精修轮详文（主文件仅存结论行）

外审按 71 页最新版复核给 86-89 分，指出四处"方法表述比真实算法高一级"的问题。本轮全部落地：

1. **Q2 主算法改名**：正文 12 处 ε-约束表述统一为"碳价参数化加权标量化（λ∈{0,200,500,900,1500} CNY/tCO₂）+ LP 对偶边际定价引导迁移"；5.2.2 重写方法动机（凸问题下与 ε-约束刻画同一前沿，价格形式与对偶定价自然耦合）；问题分析、总策略、结果段、结论、7.1 同步。图4 图注改"碳价扫描单调性检验"。
2. **Q2 真实非支配筛选**：q2_model.py 加 (cost,carbon) 二维支配检验（baseline、lam200、lam500 被筛除，非支配 = {lam0, lam1500}）；ques2_pareto_frontier.csv 加 nondominated 列；frozen pareto_point_count 5.0→2.0（label 改"候选集内非支配代表点数"）；ques2_acceptance_metrics.csv 同步；notebook cell4 与 audit 脚本逐字一致 + 筛选输出行 + frozen notebook 哈希重绑。正文图2 段写明"6 候选经支配检验保留 2 个非支配构成前沿"。
3. **Q3 循环寿命约束披露**：正文新增 Σchg ≤ N^cycle·(2407/24)·Capacity（N^cycle=1.5 次/日）+ 标定依据（无约束时 LP 套利吞吐 4050.9 GWh = 基准 587 GWh 的 6.9 倍且西部成本为负；基准反算 0.64-0.83 次/日，上浮取 1.5）+ 敏感性（1.0→约2.1亿、1.5→2.37亿、2.0→约2.6亿，结论不敏感）。
4. **Q4 降格**：全文从"六目标联合优化"改为"固定问题二迁移方案下的多情景电力层再优化"；时延/服务质量为描述性指标，非支配检验在成本-碳排-利用率三维执行（6/12 非支配不变）；MC 声明六区域聚合代理口径（情景仍逐区域精确重解）。
5. **统计术语拆分**：0.0457 全文改称"成本均值95%置信区间相对半宽（正态近似 1.96s/(√n·C̄)）"；QoS Wilson 95% CI [0.9999,1.0000] 单列；±2.94% 矛盾改 ±4.57%；6.1.3 脚注保留 0.7408 vs 0.8025 口径区分。关键词换为"多目标优化；线性规划；碳感知调度；蒙特卡洛模拟；灵敏度分析"（删无对应的"非线性规划/鲁棒优化"）。

连带修复：7.1 段被上轮压缩误截断的句子补全；7.2 缺点从"LP松弛+取整修复MILP"（旧方法，与实际不符）改写为真实局限（聚合贪心启发式非全局最优、MC 聚合代理、确定性序列处理），并删除与 Q4 矛盾的"确定性假设未考虑"句；6.3 待复算项压缩为三条一句式。

页数控制：语义扩充+约1500字曾使正文 31 页超限；先删 6 条冗余图引句导致 figure_references 条件项（全部 17 图须被正文引用），恢复后改用压缩法——6.2 优势段、6.3、问题重述四问段、7.3 第三点，最终正文回到 30 页整。

终态：res.pdf 72 页（正文 30 + 参考文献至附录 40 页）、六门禁 TECHNICAL_PASS 12/12、五件哈希 MATCH、新表述在 PDF 第 3/22 页等处渲染确认、首页完整。AI 痕迹审计 24.5 分的 P0 改进亦已在前轮完成。

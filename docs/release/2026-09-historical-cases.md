# 三历史案例定性报告（批次5，2026-09-05）

依据：批次0快照（`internal/pre_stable_20260905/index.json`）+ 批次1新审批器校准 + 批次2同步/台账检查 + 批次3状态诊断。全部只读，未执行任何 reconcile，未触碰任何交付产物。

## 定性结论

| 案例 | 状态诊断 | 新审批器 | 跨产物同步 | 定性 |
|---|---|---|---|---|
| 20260817 | CONSISTENT | **BLOCKED**（ques1/2/3 源漂移：CSV 在证据登记后被改动） | 未核（旧链无台账） | **只读历史交付**——审批不可迁移：旧 PASS 绑定的登记哈希与盘上文件已不符，不得视为当前结果的有效批准；如需复用必须先重建执行证据 |
| 20260823 | **CONTRADICTION**（completed + `quality_repair` + `repair_requested`） | PASS（20 来源，哈希一致） | 未核 | **只读历史交付 + 待状态核对**——res.json 为 `deterministic_recovery` 包装结构而非正式分节字典，不能作为 Writer 重组/恢复源；对外 completed 掩盖了未收敛的返修请求，恢复运行前必须先经 `task_state_diagnosis --reconcile`（操作人+理由） |
| 20260830 | TRANSITIONAL_EXPORT（设计内过渡态） | PASS（16 来源，哈希一致；旧 0 源 PASS 已被取代） | **FAIL**（12/12 分节与 res.md 脱节——round3 全量重构真阳性） | **只读历史交付（冻结终版）+ 待补复核（审批绑定）**——见下文专项 |

## 20260830（华数杯 v23 终版）专项

- 交付件 `res.pdf`/`res.docx` 及五件套**保持不动**（评审死命令：模型、CSV、参数、图数据、frozen 结果不再碰；本轮亦未碰）。
- 已知缺陷登记：`res.json` 仍是 Writer 自动阶段的旧方法表述（ε-约束×10），与终版 `res.md`（碳价标量化）语义脱节。**禁止将本任务 res.json 用作任何恢复、重导出或交接的内容源**——正式重导会静默带回旧方法。
- 审批绑定：checkpoint 中 `approved` 的旧 review_id 与批次1新审批器重算编号不一致，登记为**历史审批不可迁移**；新审批器对当前证据判 PASS/16 源，但批准动作须由操作人重新显式执行，不得补写。
- 若未来确需重导出，规程：①以终版 `res.md` 为准按分节回填 `res.json`；②经统一保存入口 `bump_paper_revision(origin="manual_save")` 登记台账；③重发全链（preflight→导出→视觉→审计→清单），mtime 哨兵与修订号绑定会强制整套一致。

## 08-17 补充矛盾

checkpoint `quality_review_status=not_run` 但盘上存在 PASS 审批报告（批次0发现）：报告从未被工作流消费。与"审批不可迁移"结论一致，登记不修复。

## 验收对照

计划要求"每个案例明确属于可恢复/只读历史交付/待补充复核，不再统一用 completed 掩盖差异"——三案例均已明确归类；无一例被登记为"可恢复"（08-23 需先 reconcile，08-30 需先内容同步，08-17 需先重建证据）。
